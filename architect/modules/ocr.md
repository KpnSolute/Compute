# OCR Module
Use OCR to convert invoice images into text for parsing.

## Responsibilities
- Extract text from scanned invoices
- Normalize invoice text for downstream parsing

## Python libraries
- pytesseract
- Pillow
- pdf2image if PDFs are added later
